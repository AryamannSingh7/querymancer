/**
 * Single subtle accent glow at the bottom of the viewport.
 * No grain, no stars, no candlelight — restraint is the point.
 */
export default function Atmosphere() {
  return <div className="bg-glow" aria-hidden />;
}
